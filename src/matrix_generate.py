class matrixGenerate():

    def __init__(self, fileGro, fileItp):
        # determinaX(arquivogro);
        # tiposdeatomos(arquivoitp);
        # carregaConstantes();
        # carregaAP();
        # determinaConstantes();
        
    def saveGrids():
        output = ""

        # I = gridCoulomb.length;
        # J = gridCoulomb[0].length;
        # K = gridCoulomb[0][0].length;
        # L = gridCoulomb[0][0][0].length;
        for i in xrange(0,I):
            for j in xrange(0,J):
                for k in xrange(0,K):
                    for l in xrange(0,L):
                        # saida.format("%-15g\t", gridCoulomb[i][j][k][l]);
                        # saida.format("%-15g\t", gridLJ[i][j][k][l]);
        return output
    